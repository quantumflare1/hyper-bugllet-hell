import Node from "./node.mjs";
import { keys } from "../core/input_handler.mjs";

export default class MenuControl extends Node {
	tick(ticker) {
		// FUCK HOW DO I DEAL WITH BUTTON PRESSES NOT HOLDS
	}
}